---

- name: restart network interface
  shell: ifdown {{ nic_name }} && ifup {{ nic_name }}
  register: return
  become: yes
  become_method: sudo
  listen: "restart nic"

- debug: msg="ifdown {{ nic_name }} && ifup {{ nic_name }}"
- debug: var=return

...
